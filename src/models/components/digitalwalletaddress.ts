/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";

export type DigitalWalletAddress = {
  /**
   * The first line of the address.
   */
  line1: string;
  /**
   * The second line of the address.
   */
  line2?: string | null | undefined;
  /**
   * The city for the address.
   */
  city: string;
  /**
   * The code of state, county, or province for the address in ISO 3166-2 format.
   */
  stateCode: string;
  /**
   * The zip or postal code for the address.
   */
  postalCode: string;
};

/** @internal */
export type DigitalWalletAddress$Outbound = {
  line1: string;
  line2?: string | null | undefined;
  city: string;
  state_code: string;
  postal_code: string;
};

/** @internal */
export const DigitalWalletAddress$outboundSchema: z.ZodType<
  DigitalWalletAddress$Outbound,
  z.ZodTypeDef,
  DigitalWalletAddress
> = z.object({
  line1: z.string(),
  line2: z.nullable(z.string()).optional(),
  city: z.string(),
  stateCode: z.string(),
  postalCode: z.string(),
}).transform((v) => {
  return remap$(v, {
    stateCode: "state_code",
    postalCode: "postal_code",
  });
});

export function digitalWalletAddressToJSON(
  digitalWalletAddress: DigitalWalletAddress,
): string {
  return JSON.stringify(
    DigitalWalletAddress$outboundSchema.parse(digitalWalletAddress),
  );
}
