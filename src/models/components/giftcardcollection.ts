/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  GiftCard,
  GiftCard$inboundSchema,
  GiftCard$Outbound,
  GiftCard$outboundSchema,
} from "./giftcard.js";

/**
 * Collection of results.
 */
export type GiftCardCollection = {
  /**
   * A list of items returned for this request.
   */
  items: Array<GiftCard>;
  /**
   * The number of items for this page.
   */
  limit?: number | undefined;
  /**
   * The cursor pointing at the next page of items.
   */
  nextCursor?: string | undefined;
  /**
   * The cursor pointing at the previous page of items.
   */
  previousCursor?: string | undefined;
};

/** @internal */
export const GiftCardCollection$inboundSchema: z.ZodType<
  GiftCardCollection,
  z.ZodTypeDef,
  unknown
> = z.object({
  items: z.array(GiftCard$inboundSchema),
  limit: z.number().int().default(20),
  next_cursor: z.string().optional(),
  previous_cursor: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "next_cursor": "nextCursor",
    "previous_cursor": "previousCursor",
  });
});

/** @internal */
export type GiftCardCollection$Outbound = {
  items: Array<GiftCard$Outbound>;
  limit: number;
  next_cursor?: string | undefined;
  previous_cursor?: string | undefined;
};

/** @internal */
export const GiftCardCollection$outboundSchema: z.ZodType<
  GiftCardCollection$Outbound,
  z.ZodTypeDef,
  GiftCardCollection
> = z.object({
  items: z.array(GiftCard$outboundSchema),
  limit: z.number().int().default(20),
  nextCursor: z.string().optional(),
  previousCursor: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    nextCursor: "next_cursor",
    previousCursor: "previous_cursor",
  });
});

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace GiftCardCollection$ {
  /** @deprecated use `GiftCardCollection$inboundSchema` instead. */
  export const inboundSchema = GiftCardCollection$inboundSchema;
  /** @deprecated use `GiftCardCollection$outboundSchema` instead. */
  export const outboundSchema = GiftCardCollection$outboundSchema;
  /** @deprecated use `GiftCardCollection$Outbound` instead. */
  export type Outbound = GiftCardCollection$Outbound;
}

export function giftCardCollectionToJSON(
  giftCardCollection: GiftCardCollection,
): string {
  return JSON.stringify(
    GiftCardCollection$outboundSchema.parse(giftCardCollection),
  );
}

export function giftCardCollectionFromJSON(
  jsonString: string,
): SafeParseResult<GiftCardCollection, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GiftCardCollection$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GiftCardCollection' from JSON`,
  );
}
