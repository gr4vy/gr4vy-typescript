/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ReportExecutionContext,
  ReportExecutionContext$inboundSchema,
} from "./reportexecutioncontext.js";
import {
  ReportExecutionStatus,
  ReportExecutionStatus$inboundSchema,
} from "./reportexecutionstatus.js";

export type ReportExecutionSummary = {
  /**
   * Always `report-execution`.
   */
  type: "report-execution";
  /**
   * The unique ID for the report execution.
   */
  id: string;
  /**
   * The date this report execution was created at.
   */
  createdAt: Date;
  /**
   * The date this report execution was last updated.
   */
  updatedAt: Date;
  status: ReportExecutionStatus;
  context: ReportExecutionContext;
};

/** @internal */
export const ReportExecutionSummary$inboundSchema: z.ZodType<
  ReportExecutionSummary,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: z.literal("report-execution").default("report-execution"),
  id: z.string(),
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  updated_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  status: ReportExecutionStatus$inboundSchema,
  context: ReportExecutionContext$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "created_at": "createdAt",
    "updated_at": "updatedAt",
  });
});

export function reportExecutionSummaryFromJSON(
  jsonString: string,
): SafeParseResult<ReportExecutionSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReportExecutionSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReportExecutionSummary' from JSON`,
  );
}
