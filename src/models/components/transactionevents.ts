/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  TransactionEventOutput,
  TransactionEventOutput$inboundSchema,
} from "./transactioneventoutput.js";

export type TransactionEvents = {
  /**
   * A list of items returned for this request.
   */
  items: Array<TransactionEventOutput>;
  /**
   * The number of items for this page.
   */
  limit?: number | undefined;
  /**
   * The cursor pointing at the next page of items.
   */
  nextCursor?: string | null | undefined;
  /**
   * The cursor pointing at the previous page of items.
   */
  previousCursor?: string | null | undefined;
};

/** @internal */
export const TransactionEvents$inboundSchema: z.ZodType<
  TransactionEvents,
  z.ZodTypeDef,
  unknown
> = z.object({
  items: z.array(TransactionEventOutput$inboundSchema),
  limit: z.number().int().default(20),
  next_cursor: z.nullable(z.string()).optional(),
  previous_cursor: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "next_cursor": "nextCursor",
    "previous_cursor": "previousCursor",
  });
});

export function transactionEventsFromJSON(
  jsonString: string,
): SafeParseResult<TransactionEvents, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TransactionEvents$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TransactionEvents' from JSON`,
  );
}
