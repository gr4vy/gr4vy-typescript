/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import {
    PCIForwardDefinition,
    PCIForwardDefinition$inboundSchema,
    PCIForwardDefinition$Outbound,
    PCIForwardDefinition$outboundSchema,
} from "./pciforwarddefinition.js";
import * as z from "zod";

/**
 * A definitions list of all the PCI endpoints that we can send card data to.
 */
export type PCIForwardDefinitions = {
    items?: Array<PCIForwardDefinition> | undefined;
    /**
     * The limit applied to request. This represents the number of items that are at
     *
     * @remarks
     * maximum returned by this request.
     */
    limit?: number | undefined;
    /**
     * The cursor that represents the next page of results. Use the `cursor` query
     *
     * @remarks
     * parameter to fetch this page of items.
     */
    nextCursor?: string | null | undefined;
    /**
     * The cursor that represents the next page of results. Use the `cursor` query
     *
     * @remarks
     * parameter to fetch this page of items.
     */
    previousCursor?: string | null | undefined;
};

/** @internal */
export const PCIForwardDefinitions$inboundSchema: z.ZodType<
    PCIForwardDefinitions,
    z.ZodTypeDef,
    unknown
> = z
    .object({
        items: z.array(PCIForwardDefinition$inboundSchema).optional(),
        limit: z.number().int().default(20),
        next_cursor: z.nullable(z.string()).optional(),
        previous_cursor: z.nullable(z.string()).optional(),
    })
    .transform((v) => {
        return remap$(v, {
            next_cursor: "nextCursor",
            previous_cursor: "previousCursor",
        });
    });

/** @internal */
export type PCIForwardDefinitions$Outbound = {
    items?: Array<PCIForwardDefinition$Outbound> | undefined;
    limit: number;
    next_cursor?: string | null | undefined;
    previous_cursor?: string | null | undefined;
};

/** @internal */
export const PCIForwardDefinitions$outboundSchema: z.ZodType<
    PCIForwardDefinitions$Outbound,
    z.ZodTypeDef,
    PCIForwardDefinitions
> = z
    .object({
        items: z.array(PCIForwardDefinition$outboundSchema).optional(),
        limit: z.number().int().default(20),
        nextCursor: z.nullable(z.string()).optional(),
        previousCursor: z.nullable(z.string()).optional(),
    })
    .transform((v) => {
        return remap$(v, {
            nextCursor: "next_cursor",
            previousCursor: "previous_cursor",
        });
    });

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace PCIForwardDefinitions$ {
    /** @deprecated use `PCIForwardDefinitions$inboundSchema` instead. */
    export const inboundSchema = PCIForwardDefinitions$inboundSchema;
    /** @deprecated use `PCIForwardDefinitions$outboundSchema` instead. */
    export const outboundSchema = PCIForwardDefinitions$outboundSchema;
    /** @deprecated use `PCIForwardDefinitions$Outbound` instead. */
    export type Outbound = PCIForwardDefinitions$Outbound;
}
