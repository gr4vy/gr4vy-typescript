/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives";
import * as z from "zod";

export type GetCheckoutSessionRequest = {
    /**
     * The unique ID for a Checkout Session.
     */
    checkoutSessionId: string;
};

/** @internal */
export namespace GetCheckoutSessionRequest$ {
    export const inboundSchema: z.ZodType<GetCheckoutSessionRequest, z.ZodTypeDef, unknown> = z
        .object({
            checkout_session_id: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                checkout_session_id: "checkoutSessionId",
            });
        });

    export type Outbound = {
        checkout_session_id: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, GetCheckoutSessionRequest> = z
        .object({
            checkoutSessionId: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                checkoutSessionId: "checkout_session_id",
            });
        });
}
